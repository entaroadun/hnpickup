backends:
# this will create two backends
# machines: one for ETL, the other for DM
# (you can view your machines in the google
# app engine web UI)
- name: dm
  class: B1
  instances: 1
  options: dynamic
- name: etl
  class: B1
  instances: 1
  options: dynamic

